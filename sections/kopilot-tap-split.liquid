{{ 'section-kopilot-tap-split.css' | asset_url | stylesheet_tag }}

<div class="kopilot-tap-section" id="kopilot-tap-section-{{ section.id }}">
  <div class="kopilot-tap-container">
    
    <!-- COLUMN 1: LEFT MEDIA BLOCK -->
    <div class="kopilot-tap-media-col">
      {% if section.settings.image != blank %}
        <img 
          src="{{ section.settings.image | img_url: '1500x' }}" 
          alt="{{ section.settings.heading | escape }}" 
          loading="lazy"
          class="kopilot-tap-hero-img"
        >
      {% else %}
        {{ 'image' | placeholder_svg_tag: 'kopilot-tap-hero-img placeholder-svg' }}
      {% endif %}
    </div>

    <!-- COLUMN 2: RIGHT TEXT CONTENT -->
    <div class="kopilot-tap-text-col">
      <div class="kopilot-tap-content-wrapper">
        <h2 class="kopilot-tap-heading">{{ section.settings.heading }}</h2>
      </div>
    </div>

  </div>
</div>

{% style %}
  /* --- MASTER TYPOGRAPHY & DESIGN TOKENS --- */
  #kopilot-tap-section-{{ section.id }} {
    --heading-font: "Elza", -apple-system, BlinkMacSystemFont, sans-serif;
    background-color: {{ section.settings.bg_color }};
    box-sizing: border-box;
    width: 100%;
    overflow: hidden;
    padding-top: 59px;
    padding-bottom: 59px;
    padding-left: 54px;
    padding-right: 54px;
  }

  .kopilot-tap-container {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: flex-start;
    width: 100%;
    max-width: 1332px; /* 1440px minus 54px*2 padding */
    margin: 0 auto;
    gap: 31px; /* Desktop Column Spacing Gap */
  }

  /* Column 1: Image Structure */
  .kopilot-tap-media-col {
    width: 672px; 
    height: 672px;
    flex-shrink: 0;
  }

  .kopilot-tap-hero-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  /* Column 2: Text Content Block */
  .kopilot-tap-text-col {
    flex-grow: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    max-width: 629px;
  }

  .kopilot-tap-heading {
    font-family: var(--heading-font);
    font-size: 60px;
    font-weight: 300;
    line-height: 1.1;
    letter-spacing: -1px;
    color: #000000;
    margin: 0;
    text-align: left;
  }

  /* --- RESPONSIVE LAYOUTS --- */
  @media screen and (max-width: 1439px) {
    #kopilot-tap-section-{{ section.id }} {
      padding-left: 33px;
      padding-right: 33px;
    }
    .kopilot-tap-container {
      gap: 26px;
    }
  }

  @media screen and (max-width: 991px) {
    .kopilot-tap-container {
      flex-direction: column-reverse;
      align-items: center;
    }
    .kopilot-tap-media-col {
      width: 100%;
      height: 400px;
    }
    .kopilot-tap-text-col {
      max-width: 100%;
      align-items: center;
    }
    .kopilot-tap-heading {
      text-align: center;
    }
  }

  @media screen and (max-width: 425px) {
    #kopilot-tap-section-{{ section.id }} {
      padding-top: 50px;
      padding-left: 0;
      padding-right: 0;
    }
    .kopilot-tap-text-col {
      padding-left: 33px;
      padding-right: 33px;
    }
    .kopilot-tap-heading {
      font-size: 30px;
      line-height: 1.04;
    }
    .kopilot-tap-media-col {
      height: 350px;
    }
  }
{% endstyle %}

{% schema %}
{
  "name": "Kopilot Tap Split",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Square Image"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Your critical emergency information in one tap."
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Section Background Color",
      "default": "#ffffff"
    }
  ],
  "presets": [
    {
      "name": "Kopilot Tap Split"
    }
  ]
}
{% endschema %}