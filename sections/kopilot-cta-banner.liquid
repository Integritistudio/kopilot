{% style %}
  .kopilot-cta-banner--{{ section.id }} {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }

  {% if section.settings.media_margin_top != blank %}
    .kopilot-cta-banner--{{ section.id }} .kopilot-cta-banner__media {
      margin-top: {{ section.settings.media_margin_top }}px;
    }
  {% endif %}

  /* Handle Desktop vs Mobile Image Visibility */
  .kopilot-cta-banner--{{ section.id }} .kopilot-desktop-image {
    display: block;
  }
  .kopilot-cta-banner--{{ section.id }} .kopilot-mobile-image {
    display: none;
  }

  @media screen and (max-width: 767px) {
    .kopilot-cta-banner--{{ section.id }} {
      padding-top: {{ section.settings.padding_top_mobile }}px;
      padding-bottom: {{ section.settings.padding_bottom_mobile }}px;
    }

    {% if section.settings.media_margin_top_mobile != blank %}
      .kopilot-cta-banner--{{ section.id }} .kopilot-cta-banner__media {
        margin-top: {{ section.settings.media_margin_top_mobile }}px;
      }
    {% endif %}

    /* Switch visibility on mobile devices */
    .kopilot-cta-banner--{{ section.id }} .kopilot-desktop-image {
      display: none;
    }
    .kopilot-cta-banner--{{ section.id }} .kopilot-mobile-image {
      display: block;
    }
  }
{% endstyle %}

{{ 'kopilot-base.css' | asset_url | stylesheet_tag }}
{{ 'kopilot-cta-banner.css' | asset_url | stylesheet_tag }}

<section class="kopilot-section kopilot-cta-banner kopilot-cta-banner--{{ section.id }} color-{{ section.settings.color_scheme }}">
  <div class="kopilot-cta-banner__intro">
    {% if section.settings.heading != blank %}
      <h2 class="kopilot-cta-banner__heading kopilot-heading">{{ section.settings.heading }}</h2>
    {% endif %}

    {% if section.settings.button_label != blank %}
      <a href="{{ section.settings.button_link | default: '#' }}" class="kopilot-btn kopilot-cta-banner__btn">
        {{ section.settings.button_label }}
      </a>
    {% endif %}
  </div>

  {% if section.blocks.size > 0 %}
    <div class="kopilot-cta-banner__trust">
      {% assign trust_fallbacks = 'kopilot-guarantee-moneyback.png,kopilot-guarantee-nosub.png,kopilot-guarantee-works.png' | split: ',' %}
      {% for block in section.blocks %}
        <div class="kopilot-cta-banner__trust-item" {{ block.shopify_attributes }}>
          <span class="kopilot-cta-banner__trust-icon">
            {% render 'kopilot-image',
              image: block.settings.icon,
              fallback: trust_fallbacks[forloop.index0],
              alt: block.settings.label,
              width: 64
            %}
          </span>
          {% if block.settings.label != blank %}
            <span class="kopilot-cta-banner__trust-label">{{ block.settings.label }}</span>
          {% endif %}
        </div>
      {% endfor %}
    </div>
  {% endif %}

  <div class="kopilot-cta-banner__media">
    {% render 'kopilot-image',
      image: section.settings.image,
      fallback: 'bad64fb19fb42742653b28527e5a419202fe681f.png',
      alt: section.settings.heading,
      width: 1889,
      height: 833,
      class: 'kopilot-cta-banner__image kopilot-desktop-image',
      sizes: '100vw'
    %}

    {%- assign mobile_image = section.settings.image_mobile | default: section.settings.image -%}
    {% render 'kopilot-image',
      image: mobile_image,
      fallback: 'bad64fb19fb42742653b28527e5a419202fe681f.png',
      alt: section.settings.heading,
      width: 767,
      class: 'kopilot-cta-banner__image kopilot-mobile-image',
      sizes: '100vw'
    %}
  </div>
</section>

{% schema %}
{
  "name": "Kopilot CTA banner",
  "tag": "section",
  "class": "kopilot-cta-banner-section",
  "disabled_on": { "groups": ["header", "footer"] },
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "default": "scheme-1",
      "label": "Color scheme"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Bottom image"
    },
    {
      "type": "image_picker",
      "id": "image_mobile",
      "label": "Bottom image (Mobile)"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Wherever you ride, KOPILOT.ID has your back."
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Get KOPILOT.ID"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "header",
      "content": "Media image"
    },
    {
      "type": "number",
      "id": "media_margin_top",
      "label": "Margin top (px) — Desktop",
      "info": "Leave blank to use theme default"
    },
    {
      "type": "number",
      "id": "media_margin_top_mobile",
      "label": "Margin top (px) — Mobile",
      "info": "Leave blank to use theme default"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top padding",
      "min": 0,
      "max": 80,
      "step": 4,
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom padding",
      "min": 0,
      "max": 80,
      "step": 4,
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_top_mobile",
      "label": "Top padding (mobile)",
      "min": 0,
      "max": 80,
      "step": 4,
      "default": 0
    },
    {
      "type": "range",
      "id": "padding_bottom_mobile",
      "label": "Bottom padding (mobile)",
      "min": 0,
      "max": 80,
      "step": 4,
      "default": 0
    }
  ],
  "blocks": [
    {
      "type": "trust",
      "name": "Trust item",
      "settings": [
        { "type": "image_picker", "id": "icon", "label": "Icon" },
        { "type": "text", "id": "label", "label": "Label", "default": "Money-back guarantee" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Kopilot CTA banner",
      "blocks": [
        { "type": "trust", "settings": { "label": "Money-back guarantee" } },
        { "type": "trust", "settings": { "label": "No subscription" } },
        { "type": "trust", "settings": { "label": "Works anywhere" } }
      ]
    }
  ]
}
{% endschema %}