{%- style -%}
  .ppls-{{ section.id }} {
    width: 100%;
    background-color: {{ section.settings.bg_color }};
    padding: 30px 0;
    box-sizing: border-box;
  }

  .ppls-{{ section.id }} *,
  .ppls-{{ section.id }} *::before,
  .ppls-{{ section.id }} *::after {
    box-sizing: border-box;
  }

  .ppls-{{ section.id }} .ppls-inner {
    display: flex;
    flex-direction: row;
    flex-wrap: nowrap;
    align-items: center;
    justify-content: center;
    gap: 30px;
    width: 100%;
    max-width: 1144px;
    margin: 0 auto;
    padding: 0 24px;
  }

  .ppls-{{ section.id }} .ppls-item {
    display: flex;
    align-items: center;
    justify-content: center;
    flex: 0 0 auto;
    height: 82px;
    max-height: 82px;
  }

  .ppls-{{ section.id }} .ppls-item img {
    display: block;
    width: auto;
    max-width: 165px;
    height: auto;
    max-height: 82px;
    object-fit: contain;
  }

  .ppls-{{ section.id }} .ppls-link {
    display: flex;
    align-items: center;
    justify-content: center;
    line-height: 0;
    text-decoration: none;
  }

  .ppls-{{ section.id }} .ppls-placeholder {
    display: block;
    width: 68px;
    height: 82px;
    background-color: transparent;
  }

  /* Tablet */
  @media screen and (min-width: 768px) and (max-width: 1439px) {
    .ppls-{{ section.id }} .ppls-inner {
      flex-wrap: wrap;
      gap: clamp(16px, 2.083vw, 30px);
      max-width: min(1144px, 100%);
    }

    .ppls-{{ section.id }} .ppls-item {
      height: clamp(56px, 5.694vw, 82px);
      max-height: clamp(56px, 5.694vw, 82px);
    }

    .ppls-{{ section.id }} .ppls-item img {
      max-height: clamp(56px, 5.694vw, 82px);
      max-width: clamp(90px, 11.458vw, 165px);
    }
  }

  /* Mobile — pixel perfect at 360px: 4 on top row, remaining centered below */
  @media screen and (max-width: 767px) {
    .ppls-{{ section.id }} {
      padding: 30px 0;
    }

    .ppls-{{ section.id }} .ppls-inner {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: center;
      column-gap: 20px;
      row-gap: 20px;
      max-width: 279px;
      padding: 0;
    }

    .ppls-{{ section.id }} .ppls-break {
      display: block;
      flex-basis: 100%;
      width: 100%;
      height: 0;
      margin: 0;
      padding: 0;
      overflow: hidden;
    }

    .ppls-{{ section.id }} .ppls-item {
      height: 53px;
      max-height: 53px;
    }

    .ppls-{{ section.id }} .ppls-item img {
      max-height: 53px;
      max-width: 105px;
    }

    .ppls-{{ section.id }} .ppls-placeholder {
      width: 43px;
      height: 53px;
    }
  }

  @media screen and (min-width: 768px) {
    .ppls-{{ section.id }} .ppls-break {
      display: none;
    }
  }
{%- endstyle -%}

<div class="ppls-{{ section.id }}">
  <div class="ppls-inner">
    {%- assign ppls_logo_index = 0 -%}
    {%- for block in section.blocks -%}
      {%- if block.type == 'logo' -%}
        {%- assign ppls_logo_index = ppls_logo_index | plus: 1 -%}
        <div class="ppls-item" {{ block.shopify_attributes }}>
          {%- if block.settings.image != blank -%}
            {%- assign ppls_alt = block.settings.image.alt | default: block.settings.alt_text | default: 'Partner logo' -%}
            {%- if block.settings.link != blank -%}
              <a
                class="ppls-link"
                href="{{ block.settings.link }}"
                {% if block.settings.open_new_tab %}target="_blank" rel="noopener noreferrer"{% endif %}
              >
                {{
                  block.settings.image
                  | image_url: width: 400
                  | image_tag: loading: 'lazy', alt: ppls_alt
                }}
              </a>
            {%- else -%}
              {{
                block.settings.image
                | image_url: width: 400
                | image_tag: loading: 'lazy', alt: ppls_alt
              }}
            {%- endif -%}
          {%- else -%}
            <span class="ppls-placeholder" aria-hidden="true"></span>
          {%- endif -%}
        </div>
        {%- if ppls_logo_index == 4 -%}
          <div class="ppls-break" aria-hidden="true"></div>
        {%- endif -%}
      {%- endif -%}
    {%- endfor -%}
  </div>
</div>

{% schema %}
{
  "name": "Partners Logo Strip",
  "tag": "section",
  "class": "section-partners-logo-strip",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background color",
      "default": "#FFFFFF"
    }
  ],
  "blocks": [
    {
      "type": "logo",
      "name": "Logo",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Logo image"
        },
        {
          "type": "text",
          "id": "alt_text",
          "label": "Alt text",
          "default": "Partner logo"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link (optional)"
        },
        {
          "type": "checkbox",
          "id": "open_new_tab",
          "label": "Open link in new tab",
          "default": true
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Partners Logo Strip",
      "blocks": [
        { "type": "logo" },
        { "type": "logo" },
        { "type": "logo" },
        { "type": "logo" },
        { "type": "logo" },
        { "type": "logo" },
        { "type": "logo" }
      ]
    }
  ]
}
{% endschema %}
